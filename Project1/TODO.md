Report.java must:
[ ]Allow the user to select an input file using JFileChooser.
[ ]Display the results in a JTable.
[ ]Display one row for each data set size.
[ ]Display one row for each data set size.
The table must contain five columns:
Column Description
[ ] 1 Data set size
[ ]2 Average critical operation count
[ ]3 Coefficient of variation (%) for critical operation counts
[ ]4 Average execution time
[ ]5 Coefficient of variation (%) for execution times

NOTE: The coefficient of variation values should be expressed as percentages. The table title should display the
name of the selected input file. Column alignment should be right aligned except for the first column and
column titles.